<!DOCTYPE html>
<html>
  <head>
    <title>Foster Roster</title>
    <%= favicon_link_tag asset_path('favicon.jpg'), rel: 'icon', type: 'image/jpg' %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <nav class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">
          <%= link_to 'Foster Roster', admin_organization_users_path(organization_slug: @organization.slug), class: 'navbar-brand' %>
        </div>
        <ul class="nav navbar-nav">
          <li><%= link_to 'Apply Filters', show_filters_admin_organization_users_path(organization_slug: @organization.slug) %></li>
          <li><%= link_to 'Users', admin_organization_users_path(organization_slug: @organization.slug) %></li>
          <li><%= link_to 'Outreaches', admin_outreaches_path %></li>
          <li><%= link_to 'Download Entire Roster', download_csv_admin_organization_users_path(organization_slug: @organization.slug), method: :post %></li>
        </ul>
        <%= form_tag search_admin_organization_users_path(slug: @organization.slug), method: :get, class: 'navbar-form navbar-left' do %>
          <div class="form-group">
            <%= text_field_tag 'user_search', nil, class: 'form-control', placeholder: 'Search' %>
          </div>
          <%= submit_tag 'Submit', class: 'btn btn-default' %>
        <% end %>
        <p class="navbar-text navbar-right">Signed in as <a href="#" class="navbar-link"><%= @organization.name %></a></p>
      </div>
    </nav>
    <div class="container">
      <%- if flash[:notice] %>
        <div class="alert alert-success"><%= flash[:notice] %></div>
      <% end %>
      <%- if flash[:alert] %>
        <div class="alert alert-warning"><%= flash[:alert] %></div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
