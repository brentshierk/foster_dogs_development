= form_tag admin_users_path do
  = hidden_field_tag 'survey[uuid]', @organization.survey.uuid
  = hidden_field_tag 'organization_slug', @organization.slug
  .fr-form
    = render partial: 'shared/basic_user_info', locals: { user: @user }
